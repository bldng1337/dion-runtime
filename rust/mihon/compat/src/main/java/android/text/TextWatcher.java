package android.text;

/**
 * Minimal {@code android.text.TextWatcher} stub.
 *
 * <p>Some extensions implement {@code TextWatcher} on helper objects wired to
 * preference {@code EditText} fields. The interface needs to exist for the
 * extension class to load; the callback methods are never invoked on the JVM.
 */
public interface TextWatcher {
}
